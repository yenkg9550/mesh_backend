# 雲端部署：後端連接 MongoDB Atlas
# 使用方式：docker compose -f docker-compose.prod.yml up -d
services:
  server:
    build: .
    container_name: mesh-server-prod
    ports:
      - "3000:3000"
    env_file:
      - .env.prod        # 放 Atlas 連線字串，不要 commit 進 git
    restart: unless-stopped
